import React from 'react';

const ComplaintHistory = ({ history }) => (
  <div className="complaint-history">
    {/* render past complaints */}
    ComplaintHistory
  </div>
);

export default ComplaintHistory;
